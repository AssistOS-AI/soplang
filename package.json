{
  "name": "soplang",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "rm -rf Persisto && git clone https://github.com/OpenDSU/Persisto.git && cd Persisto && npm install"
  },
  "private": true,
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@eslint/json": "^0.12.0",
    "eslint": "^9.25.1",
    "globals": "^16.0.0"
  },
  "dependencies": {
    "soundpubsub": "github:opendsu/soundpubsub"
  }
}
